<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>合作金庫商業銀行 Copilot</title>
    <style>
        /* 恢復 body 正常設定，只保留背景色與字體，不干擾其他 HTML */
        body {
            margin: 0;
            min-height: 100vh;
            background: linear-gradient(135deg, #f4f7f6 0%, #eaeceb 100%);
            font-family: "Microsoft JhengHei", sans-serif;
        }

        /* 只有這個區塊會被「獨立抽出來」並上下左右絕對置中 */
        .cooperative-text {
            position: absolute; /* 絕對定位，脫離正常文件流 */
            top: 50%; /* 距離上方 50% */
            left: 50%; /* 距離左側 50% */
            transform: translate(-50%, -50%); /* 往回推自身長寬的一半，達到完美置中 */
            /* 外觀樣式維持不變 */
            font-size: 2.2rem;
            font-weight: bold;
            color: #00703c; /* 合庫經典綠 */
            letter-spacing: 2px;
            padding: 20px 40px;
            border: 2px solid #bba155; /* 合庫金黃色 */
            border-radius: 8px;
            background-color: #ffffff;
            box-shadow: 0 4px 15px rgba(0, 112, 60, 0.1);
            text-align: center;
            white-space: nowrap; /* 確保文字保持在一行 */
        }

        /* 如果您有其他的 HTML (例如 share html)，可以在這裡寫它們的樣式 */
        .other-content {
            padding: 20px;
        }
    </style>
</head>
<body>

    <div class="other-content">
    </div>
    <div class="cooperative-text">Copilot PowerAutomate 介接應用</div>
</body>
</html>